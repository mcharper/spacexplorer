import { makeStyles } from "@material-ui/core";

export const useLaunchListerStyles = makeStyles({
  launchList: {
    height: "50vh",
    marginLeft: "2vw",
    padding: "0.5vw",
    width: "50vw",
  },
});
